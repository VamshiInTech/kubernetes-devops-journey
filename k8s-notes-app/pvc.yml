apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-pvc
spec:
  storageClassName: ""   # 👈 THIS LINE IS KEY
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
